{
  "name": "tw-incremental",
  "private": true,
  "version": "0.47.0",
  "type": "module",
  "description": "Incremental/idle game set in the Tribal Wars world — static, client-only, graphics drawn purely in code.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "sim": "tsx sim/index.ts"
  },
  "dependencies": {
    "break_infinity.js": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.8"
  }
}
